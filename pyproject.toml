[project]
name = "fah-dev-macos"
version = "0.1.1"
description = "Folding@home Development Environment for macOS"
license = "CC0-1.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
dependencies = [
    "scons>=4.10.1",
    "six>=1.17.0",
]

[tool.uv]
required-version = ">=0.9.0"
